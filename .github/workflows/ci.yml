name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

env:
  NODE_VERSION: 24

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - run: npm install

      - name: TypeScript
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

      - name: Format
        run: npm run format:check

      - name: Unit tests
        run: npm run test:run

      - name: Build
        run: npm run build
        env:
          DATABASE_URL: postgres://localhost:5432/dummy

  e2e-smoke:
    runs-on: ubuntu-latest
    needs: [validate]
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: agentclinic
          POSTGRES_PASSWORD: agentclinic
          POSTGRES_DB: agentclinic
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - run: npm install

      - name: Run migrations
        run: npm run db:migrate
        env:
          DATABASE_URL: postgres://agentclinic:agentclinic@localhost:5432/agentclinic

      - name: Seed database
        run: npm run db:seed
        env:
          DATABASE_URL: postgres://agentclinic:agentclinic@localhost:5432/agentclinic

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Start dev server
        run: npm run dev &
        env:
          DATABASE_URL: postgres://agentclinic:agentclinic@localhost:5432/agentclinic

      - name: Wait for dev server
        run: |
          for i in {1..30}; do
            curl -s http://localhost:3000 > /dev/null 2>&1 && break
            sleep 2
          done

      - name: E2E smoke test
        run: npx playwright test e2e/smoke.spec.ts --reporter=list
        env:
          DATABASE_URL: postgres://agentclinic:agentclinic@localhost:5432/agentclinic
