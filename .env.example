# Database (local development)
DATABASE_URL=postgres://agentclinic:agentclinic@localhost:5432/agentclinic

# Production (Vercel + Neon)
# DATABASE_URL is auto-injected by the Neon / Vercel Postgres integration
# For local testing with a production-like connection:
# DATABASE_URL=postgres://agentclinic:<strong-password>@<host>:5432/agentclinic
